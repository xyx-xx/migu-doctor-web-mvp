# 咪咕医生 — AI 协作上下文

开始任务前，先阅读：

- `docs/ai-context/spec.md`
- `docs/ai-context/progress.md`
- `docs/ai-context/project-structure.md`

## 产品定位

咪咕医生是面向宠物主的宠物健康 Web MVP，核心价值是结构化采集信息、识别紧急信号并给出下一步行动提示。它不做诊断，不替代执业兽医。

## 项目负责人背景

- 动物医学本科在读，负责场景判断、需求拆解、产品取舍和验收。
- 通过 Claude Code、Codex 等工具协作完成设计与工程实现。
- AI 是工作流中的研究、实现和审查伙伴；产品负责人对事实核验、安全边界与最终交付负责。
- 技术说明优先使用清楚、可验证的语言，并解释关键取舍。

## 现有真实能力

- 三步症状信息采集与危险信号规则分流
- AI 追问和结构化风险提示
- 误食风险信息采集与高风险转诊提示
- 日常养宠百科
- 附近宠物医院导航入口
- 本地 Python 代理、错误处理和安全文本渲染

账号、云数据库、多宠物档案、通知和正式部署均未实现，不得写成已完成。

## 工作规则

1. 先确认当前实现和需求证据，再修改代码。
2. 移动端优先，触控目标和文字必须清晰可读。
3. 每次只交付一个可验收模块；修改后做语法、流程和回归检查。
4. 模型输出不得直接注入 `innerHTML`；优先使用 DOM 与 `textContent`。
5. 密钥只保存在 `.env`，不得进入前端、日志、截图或 Git。
6. 医疗高风险判断优先使用确定性规则；信息不足时保守转诊。
7. 禁止建议自行催吐、处方用药、强行喂食或弱化危险信号。
8. 变更产品范围、数据结构或安全边界时同步更新上下文文档。
9. 不运行强制推送、递归删除等破坏性操作。

## 技术栈

- 前端：HTML、CSS、原生 JavaScript
- 本地服务：Python `ThreadingHTTPServer`
- 模型接口：DeepSeek（OpenAI 兼容 SDK）
- 本地状态：`localStorage`
